import React from 'react';
import { render } from '@testing-library/react';
import App from './App';

// test('renders App root', () => {
//   const { getById } = render(<App />);
//   const appRoot = getbyI(/learn react/i);
//   expect(linkElement).toBeInTheDocument();
// });
